**EN** | [PL](README.pl.md)

# The Works of Jonathan Edwards

![Portrait of Jonathan Edwards](assets/jonathan-edwards.svg)

This project creates searchable Markdown files from locally archived
[WJE Online](http://edwards.yale.edu/research/browse) pages for the 73 volumes
of *The Works of Jonathan Edwards*. For volumes 17–73, the controlled import
tool uses Microsoft Edge through Playwright/CDP, saves the rendered DOM and
resources observed in the same browser session, and requires no macOS
Accessibility permission. Direct HTTP downloads, APIs, MHTML, and editing the
saved HTML are not allowed; conversion remains entirely local.

## Contents

- `HTML/VOLUMENN/` — unmodified, locally saved source pages. `000.html`
  contains the volume navigation and heading hierarchy.
- `MD/VOLUMENN.md` — the resulting Markdown text for a volume; volumes 1–9
  use a leading zero (for example, `MD/VOLUME01.md`).
- `assets/` — project graphics used by documentation.
- `scripts/html_volume_to_markdown.rb` — a converter that tolerates the
  archive's malformed HTML.
- `scripts/archive_yale_volume.mjs` — the only permitted importer for new
  Yale source pages; it uses the same Playwright/CDP browser mechanism as the
  code-architect preview and requires Microsoft Edge on macOS.
- `scripts/audit_volume_images.rb` and `scripts/archive_and_convert_volume.rb`
  — deterministic image selection and selective conversion for new volumes.
- `AGENTS.md` — the working procedure for subsequent volumes.

The output preserves source content, heading structure, footnotes, and page
numbers. Page numbers are intentionally stored as unobtrusive comments, for
example `<!-- p. 123 -->`, so that they remain searchable without disrupting
the document hierarchy. Images, site navigation, and footers are normally
omitted. When illustrations are essential, the optional `--include-images`
mode copies the locally saved content images to `MD/assets/VOLUMENN/` and uses
relative paths in the Markdown file.

## Creating a volume

For the full controlled import, resume, image-manifest, conversion, and
selective-verification procedure for volumes 17–73, follow `AGENTS.md`. Once
the unmodified source files are present in `HTML/VOLUMENN/`, conversion can be
run with:

```bash
ruby scripts/html_volume_to_markdown.rb HTML/VOLUMENN MD/VOLUMENN.md
```

For example, for the second volume:

```bash
ruby scripts/html_volume_to_markdown.rb HTML/VOLUME02 MD/VOLUME02.md
```

For a volume whose content images must be preserved:

```bash
ruby scripts/html_volume_to_markdown.rb --include-images HTML/VOLUMENN MD/VOLUMENN.md
ruby scripts/verify_volume_markdown.rb --include-images HTML/VOLUMENN MD/VOLUMENN.md
```

The converter reads local HTML only, identifies main content from the archive's
comments, derives heading levels from `000.html`, and creates unique Markdown
footnotes. Uniqueness matters because printed footnote numbering may restart in
different source fragments.

## Verification

After generating a volume, run at least:

```bash
ruby -c scripts/html_volume_to_markdown.rb
ruby scripts/verify_volume_markdown.rb HTML/VOLUMENN MD/VOLUMENN.md
git diff --check
```

The validator compares page markers, footnote references, and footnote
definitions with the local HTML, and reports headings from `000.html` that are
absent from the result. A gap in page numbering means the missing fragment
should be retried with the controlled importer and `--resume`; it must not be
reconstructed from memory or another edition.

## Capture status

| Volume | Title | Markdown file | Source status |
| --- | --- | --- | --- |
| 1 | *Freedom of the Will* | [MD/VOLUME01.md](MD/VOLUME01.md) | The saved content includes the complete hierarchy from `000.html`. Page markers 31, 136, and 149 are absent from the source; the document does not add them artificially. |
| 2 | *Religious Affections* | [MD/VOLUME02.md](MD/VOLUME02.md) | The saved content includes the complete hierarchy from `000.html`. Page markers 46, 76–77, 84, 125, and 440 are absent from the source; the document does not add them artificially. `007.html` lacks the archive's usual closing comment, so the converter uses a safe fallback end. |
| 3 | *Original Sin* | [MD/VOLUME03.md](MD/VOLUME03.md) | The saved content includes the complete hierarchy from `000.html`. Page markers 105–106, 220–222, 350–352, and 372–374 are absent from the source; the document does not add them artificially. `007.html` lacks the archive's usual closing comment, so the converter uses a safe fallback end. |
| 4 | *The Great Awakening* | [MD/VOLUME04.md](MD/VOLUME04.md) | The saved content includes the complete hierarchy from `000.html` and page markers 1–570 without gaps. |
| 5 | *Apocalyptic Writings* | [MD/VOLUME05.md](MD/VOLUME05.md) | The saved content includes the complete hierarchy from `000.html` and page markers 1–464 without gaps. The navigation notes that Edwards did not comment on Revelation 3 in the exposition. |
| 6 | *Scientific and Philosophical Writings* | [MD/VOLUME06.md](MD/VOLUME06.md) | The saved content includes the complete hierarchy from `000.html`. Page markers 1 and 144–146, 170–171, and 311 are absent from the source; the document does not add them artificially. The locally saved content images are preserved in `MD/assets/VOLUME06/`. |
| 7 | *The Life of David Brainerd* | [MD/VOLUME07.md](MD/VOLUME07.md) | The saved content includes the complete hierarchy from `000.html`, with page markers 1–590 and front-matter markers viii–x. |
| 8 | *Ethical Writings* | [MD/VOLUME08.md](MD/VOLUME08.md) | The saved content includes the complete hierarchy from `000.html`. Page markers 122–124, 127–128, 398–400, 403–404, 416–418, 455, 464–466, 507, 537–538, 628–630, 641–642, 651, 668, 672, 678, and 688 are absent from the source; the document does not add them artificially. |
| 9 | *A History of the Work of Redemption* | [MD/VOLUME09.md](MD/VOLUME09.md) | The saved content includes the complete hierarchy from `000.html`, with page markers 1–556 and front-matter markers vii–ix. Images are omitted. |
| 10 | *Sermons and Discourses 1720–1723* | [MD/VOLUME10.md](MD/VOLUME10.md) | The saved content includes the complete hierarchy from `000.html`. Page markers 2, 259–260, 578, and 644 are absent from the source; the document does not add them artificially. Two source diagrams are transcribed as Mermaid; only `jec-yje10-100.jpg` is retained as an image. |
| 11 | *Typological Writings* | [MD/VOLUME11.md](MD/VOLUME11.md) | The local capture includes source files `001.html`–`010.html`. Page markers 2, 36, 117, 144, 154, 156, and 190 are absent from the source; the document does not add them artificially. The manuscript-structure image on source page 004 is transcribed as Mermaid; all images are otherwise omitted. |
| 12 | *Ecclesiastical Writings* | [MD/VOLUME12.md](MD/VOLUME12.md) | The local capture includes source files `001.html`–`007.html`. Page markers 92, 164, 166, 350, 504, and 506 are absent from the source; the document does not add them artificially. Images are omitted. |
| 13 | *The "Miscellanies": (Entry Nos. a–z, aa–zz, 1–500)* | [MD/VOLUME13.md](MD/VOLUME13.md) | The local capture includes source files `001.html`–`005.html`. Page markers 110–112, 124, 151–152, 161–162, and 342–544 are absent from the source; the document does not add them artificially. Twenty-six valid local images are preserved in `MD/assets/VOLUME13/`; the figure on source page 002 was saved as HTML rather than an image and is omitted. |
| 14 | *Sermons and Discourses: 1723–1729* | [MD/VOLUME14.md](MD/VOLUME14.md) | The local capture includes source files `001.html`–`024.html`, with continuous Arabic page markers 1–550. Images are omitted. |
| 15 | *Notes on Scripture* | [MD/VOLUME15.md](MD/VOLUME15.md) | The local capture includes source files `001.html`–`003.html`. Page markers 17, 47, and 48 are absent from the source; the document does not add them artificially. Images are omitted. |
| 16 | *Letters and Personal Writings* | [MD/VOLUME16.md](MD/VOLUME16.md) | The local capture includes source files `001.html`–`075.html`. Arabic page markers 3–837 are present except 28–29, 32, 34, 39, 71, 85, 88, 101, 111, 134, 143–144, 148, 152, 173, 199, 224, 255, 259, 266, 282, 296, 394, 436, 449, 485, 512, 739–740, and 805–806; the document does not add them artificially. Images are omitted. |
| 17 | *Sermons and Discourses, 1730–1733* | [MD/VOLUME17.md](MD/VOLUME17.md) | The browser capture includes `000.html`, 21 top-level navigation entries, and source files `001.html`–`022.html` (`Contents` is archived separately from the 21 `navlevel1` entries). Front-matter markers vii–xii and Arabic page markers 1–458 are continuous. `022.html` lacks the archive's usual closing comment, so the converter uses the captured footer as a safe end; no content gap is visible. The image audit found 50 candidates: 47 non-content images and 3 uncertain content images (`003:archive`, `003:archive(1)`, and `012:archive(1)`); none are classified `include`, so no images are embedded or copied. |
| 18 | *The "Miscellanies," (Entry Nos. 501–832)* | [MD/VOLUME18.md](MD/VOLUME18.md) | The browser capture includes `000.html` and source files `001.html`–`089.html`: four aggregate sections plus 85 individually recovered entries 749–832. Yale's aggregate `004.html` response ends in the middle of entry 748 after page marker 392; the visible child pages recover entries 749–832, but the end of entry 748 remains absent and is not reconstructed. Front-matter markers occur in the saved order vii, x, ix, x, xi. Arabic markers cover 1–547, with 288 repeated and 289 absent in the source. `089.html` lacks the usual closing comment, but its safe fallback ends after complete entry 832 and page 547. The image audit classified all 183 candidates as `omit-noncontent`; no images are embedded or copied. |
| 19 | *Sermons and Discourses, 1734–1738* | [MD/VOLUME19.md](MD/VOLUME19.md) | The browser capture includes `000.html` and source files `001.html`–`036.html`. Front-matter markers viii–xiv are continuous. Arabic markers run from 3–811; markers 1–2 and 792 are absent from the source and are not added. The navigation label `[intro]` is a technical grouping placeholder rather than a content heading; all of its child headings are present. `036.html` lacks the usual closing comment, but its safe fallback ends after Appendix B and page 811. The image audit classified all 74 candidates as `omit-noncontent`; no images are embedded or copied. |
| 20 | *The "Miscellanies," 833–1152* | [MD/VOLUME20.md](MD/VOLUME20.md) | The browser capture includes `000.html` and source files `001.html`–`068.html`: three initial sections plus 65 individually recovered entries 1085–1152. Yale's aggregate `003.html` response ends in the middle of entry 1084 after page marker 467; the visible child pages recover entries 1085–1152, but the end of entry 1084 remains absent and is not reconstructed. Front-matter markers vii–xi are continuous. Arabic markers cover 1–525 without duplicates, except that 40–42 and 468 are absent from the source. Navigation labels 840a, 861, 1137, and 1150 differ from their saved content headings, but the corresponding entries are present. `068.html` lacks the usual closing comment, but its safe fallback ends after complete entry 1152; the last captured page marker is 525. The image audit classified all 138 candidates as `omit-noncontent`; no images are embedded or copied. |
| 21 | *Writings on the Trinity, Grace, and Faith* | [MD/VOLUME21.md](MD/VOLUME21.md) | The browser capture includes `000.html` and source files `001.html`–`015.html`, matching all 15 visible top-level navigation entries. Front-matter markers occur as ii and vii–xii. Arabic markers run from 1–524 without duplicates, except that 107–108 are absent from the Yale source. The printed contents list a General Index at page 525 and an Index of Biblical Passages at page 545, but `000.html` provides no navigation entries for either; the absent index material is not reconstructed. Five content `<img>` elements in `002.html`, `003.html`, `008.html`, `011.html`, and `012.html` all point to Yale's literal `NAME OF FILE.jpg` placeholder, so the frontispiece and four manuscript-page illustrations have no captured image file and are not reconstructed. The image audit classified all 32 locally saved candidates as `omit-noncontent`; no images are embedded or copied. Source `011.html` also contains an unreferenced footnote definition, preserved explicitly as `[^011-note4]` (`review me. Romans 17`). `015.html` lacks the usual closing comment, but its safe fallback reaches the complete final visible section and page 524 before the captured footer. |
| 22 | *Sermons and Discourses, 1739–1742* | [MD/VOLUME22.md](MD/VOLUME22.md) | The browser capture includes `000.html` and source files `001.html`–`065.html`, matching all 65 visible top-level navigation entries. Front-matter markers vii–xiv are continuous. Arabic markers run from 1–552 without duplicates, except for illustration pages 36 and 419. Four content `<img>` elements in `005.html`, `033.html`, `039.html`, and `048.html` point to manuscript scans at printed pages 36, 266, 323, and 419, but the corresponding image responses were not captured as local files; these scans are not reconstructed or embedded. The printed contents list a General Index at page 553 and an Index of Biblical Passages at page 573, but `000.html` provides no navigation entries for either, so that absent index material is not reconstructed. The technical navigation labels `[transcription]` and `[renditional]` do not appear as literal Markdown headings; their substantive child headings and text are present. Manuscript folio markers in the transcription are preserved. The image audit classified all 132 locally saved candidates as `omit-noncontent`; no images are embedded or copied. `065.html` lacks the usual closing comment, but its safe fallback reaches the complete final visible appendix and page 552 before the captured footer. |
| 23 | *The "Miscellanies," (Entry Nos. 1153–1360)* | [MD/VOLUME23.md](MD/VOLUME23.md) | The browser capture includes `000.html` and source files `001.html`–`004.html`, matching all four visible top-level navigation entries. The visible `Front Matter` source is title-only, and the first saved page markers are 38–39; printed pages 1–37 are unavailable in Yale's navigation and are not reconstructed. The remaining Arabic markers form a continuous, duplicate-free sequence from 38–716. Aggregate `002.html` contains complete entries 1153–1350 through page 461, `003.html` contains entry 1351 on pages 462–481, and `004.html` contains entries 1352–1360 through their complete conclusion on page 716, so descendant recovery was not needed. Yale's navigation labels for entries 1264 and 1325 stop mid-heading, while the corresponding full headings and text are present in Markdown. One unreferenced source footnote is preserved explicitly as `[^002-note417]` with its `review me` editorial note. Four manuscript-page scans are classified as `omit-scan`; the other ten candidates are `omit-noncontent`, with no `include` or `uncertain`, so no images are embedded or copied. All four GFM tables validate. `004.html` lacks the usual closing comment, but its safe fallback ends after the complete final paragraph and page 716 before the captured footer. |
| 24 | *The "Blank Bible"* | [MD/VOLUME24.md](MD/VOLUME24.md) | The browser capture includes `000.html` and source files `001.html`–`006.html`, matching all six visible top-level navigation entries. The printed contents list a *General Index* at page 1267, an *Index of Biblical Passages* at page 1323, and an *Index of "Blank Bible" Entries* at page 1395, but `000.html` provides no navigation entry for any of them; the absent indexes are not reconstructed. Front-matter markers occur as vii–ix and xi–xiii, with x absent. Arabic markers span 1–1266: 22, 107–108, and 111–112 are absent even though the listed table content is present; 118–122 and 822–823 are absent at section boundaries; 128 is repeated while 138 is absent, and 1235 is repeated while 1255 is absent. The technical `[intro]` label is a grouping placeholder, and all substantive child headings are present. The frontispiece in `002.html` points to an uncaptured Yale image URL. Seven locally captured manuscript scans are classified as `omit-scan` and fourteen interface images as `omit-noncontent`; `include=0` and `uncertain=0`, so no images are embedded or copied. All 33 GFM tables and 312 footnote pairs validate. `006.html` lacks the usual closing comment, but its safe fallback ends after complete Appendix C and page 1266 before the captured footer. |
| 25 | *Sermons and Discourses, 1743–1758* | [MD/VOLUME25.md](MD/VOLUME25.md) | The browser capture includes `000.html` and source files `001.html`–`039.html`, matching all 39 visible top-level navigation entries. The printed contents list an Editorial Committee at page v, but the visible front matter jumps from ii to vii and provides neither that section nor a navigation entry. They also list a *General Index* at page 761 and an *Index of Biblical Passages* at page 783, while `000.html` ends with the appendix and exposes neither index; none of this absent material is reconstructed. Front-matter markers occur as ii and vii–xiv. Every Arabic marker 1–760 is present exactly once, although malformed table markup in `039.html` moves some standalone markers before markers inside cells and causes a DOM-order inversion from 760 to 718 without a pagination gap. The technical `[intro]` label, a truncated Farewell Sermon navigation title, and an appendix label missing its date range differ from the complete source headings retained in Markdown. The image audit found 86 candidates: the frontispiece map is `include` and is embedded as `MD/assets/VOLUME25/002-archive.jpg`, five manuscript pages are `omit-scan`, eighty interface images are `omit-noncontent`, and `uncertain=0`. Both GFM tables, all 574 footnote pairs, and all 769 page markers validate. `039.html` lacks the usual closing comment, but its safe fallback ends after complete appendix entry 1225 and page 760 before the captured footer. |
| 26 | *Catalogues of Books* | [MD/VOLUME26.md](MD/VOLUME26.md) | The browser capture includes `000.html` and source files `001.html`–`008.html`, matching all eight visible top-level navigation entries. Both previous-section links in `001.html` target the existing Yale identifier `c.25:0.wjeo`, but that section is absent from `000.html`, the manifest, and the local files; this unavailable pre-page-1 front matter was not reconstructed. The saved Arabic markers 1–370 are continuous, unique, and ordered. Unpaginated `002.html` is the technical `[NA]` title section, and `003.html` continues directly from page 95 after `001.html` ends at page 94. Navigation labels `[NA]` and `[1]`, the truncated `ternal References and Book Publication Dates)` label, and malformed nested headings are source-markup anomalies; notes and the two printed tables are flattened as source prose rather than semantic footnotes or HTML tables. The image audit classified all eighteen candidates as `omit-noncontent`; `include=0` and `uncertain=0`, so no assets were copied. All 370 page markers validate. `008.html` lacks the usual closing comment, but its safe fallback contains the complete Index of Biblical Passages through the final Revelation entry and then the captured footer. |
| 27 | *"Controversies" Notebook* | [MD/VOLUME27.md](MD/VOLUME27.md) | The browser capture includes `000.html` and source files `001.html`–`005.html`, matching all five visible top-level navigation entries. Saved Previous/Next links expose Yale sections `c.26:0`, `c.26:2`, and `c.26:3`, but those sections are absent from `000.html`, the manifest, and the local files; their unavailable content was not reconstructed. These manuscript pages contain no standard printed page markers. All 474 semantic footnote references have definitions, while nine source definitions without semantic references remain explicitly preserved. Four sources contain empty `transcriptImage` placeholders without URLs or local files; the image audit classified all twelve locally saved interface candidates as `omit-noncontent`, with no `include` or `uncertain`. `005.html` lacks the usual closing comment, but its safe fallback reaches the complete Part V conclusion and note 119 before the footer. |
| 28 | *Minor Controversial Writings* | [MD/VOLUME28.md](MD/VOLUME28.md) | The browser capture includes `000.html` and source files `001.html`–`005.html`, matching all five visible entries `c.27:1`–`c.27:5`. The Previous link from `001.html` exposes `c.27:0`, but that preceding section is absent from `000.html`, the manifest, and the local files; its unavailable content was not reconstructed. These manuscript pages contain no standard printed page markers. All 171 semantic footnote references have definitions, while two nested parenthetical source definitions without semantic references remain explicitly preserved. Sources `002.html`–`005.html` contain empty `transcriptImage` placeholders without URLs or local files; the image audit classified all twelve locally saved interface candidates as `omit-noncontent`, with no `include` or `uncertain`. `005.html` lacks the usual closing comment, but its safe fallback reaches the complete final *Subjects of Inquiry* entry and editorial note before the footer. |
| 29 | *"Harmony of the Scriptures"* | [MD/VOLUME29.md](MD/VOLUME29.md) | The browser capture includes `000.html` and the sole visible source `001.html` (`c.28:1`), preserving the complete *Extracts out of the Bishop of London's Third Pastoral Letter*. Its Previous link exposes `c.28:0`, but that preceding section is absent from `000.html`, the manifest, and the local files; its unavailable content was not reconstructed. The saved manuscript page contains no standard printed page markers, semantic footnote definitions, or content table; four inline editorial and marginal notes are retained as source prose. Its one empty `transcriptImage` placeholder has no URL or local file; the image audit classified all four locally saved interface candidates as `omit-noncontent`, with no `include` or `uncertain`. `001.html` lacks the usual closing comment, but its safe fallback reaches the complete final extract and the source notation `[blank] [4]` before the footer. |
| 30 | *"Prophecies of the Messiah"* | [MD/VOLUME30.md](MD/VOLUME30.md) | The browser capture includes `000.html` and all nine visible navigation entries as `001.html`–`009.html` (`c.29:1`–`c.29:9`). The Previous link from `001.html` exposes `c.29:0`, but that preceding section is absent from `000.html`, the manifest, and the local files; its unavailable content was not reconstructed. These manuscript pages contain no standard printed page markers. All 436 semantic footnote references have definitions, and no content tables are present. Each source has an empty `transcriptImage` placeholder without a URL or local file; the image audit classified all twenty locally saved candidates as `omit-noncontent`, with no `include`, `omit-scan`, or `uncertain`. `009.html` lacks the usual closing comment, but its safe fallback reaches the complete final visible prophecy list before the footer. |
| 31 | *"History of Redemption" Notebooks* | [MD/VOLUME31.md](MD/VOLUME31.md) | The browser capture includes `000.html` and all three visible books as `001.html`–`003.html` (`c.30:1`–`c.30:3`). The Previous link from `001.html` exposes `c.30:0`, but that preceding section is absent from `000.html`, the manifest, and the local files; its unavailable content was not reconstructed. The manuscript transcription uses literal bracketed leaf and column notations rather than standard semantic page markers. All 164 semantic footnote references have definitions. Each book has an empty `transcriptImage` placeholder without a URL or local file; the image audit classified all eight locally saved candidates as `omit-noncontent`, with no `include`, `omit-scan`, or `uncertain`. `003.html` lacks the usual closing comment, but its safe fallback reaches the complete final visible 755 entry before the footer. |
| 32 | *Correspondence by, to, and about Edwards and His Family* | [MD/VOLUME32.md](MD/VOLUME32.md) | The browser capture includes `000.html` and all 561 visible correspondence entries as `001.html`–`561.html`; Yale identifiers `c.31:1`–`c.31:561` are continuous and unique. The Previous link from `001.html` exposes `c.31:0`, but that preceding section is absent from `000.html`, the manifest, and the local files. Eight visible entries (`411`, `500`, `543`, `550`, `553`, `555`, `557`, and `561`) contain only Yale's literal `[Text forthcoming]` placeholder; none of this unavailable text was reconstructed. Nineteen specified sources contain empty `transcriptImage` placeholders without URLs or local files. The transcription has no standard semantic page markers and preserves the source's explicit fragments, manuscript damage, and lacunae. All 1,872 semantic footnote references have definitions; sixteen additional unreferenced source definitions are retained explicitly. The image audit classified all 1,124 locally saved candidates as `omit-noncontent`, with no `include`, `omit-scan`, or `uncertain`. `561.html` lacks the usual closing comment, but its safe fallback reaches the final visible spurious-letter heading and `[Text forthcoming]` placeholder before the footer. |
| 33 | *"Misrepresentations Corrected" Draft* | [MD/VOLUME33.md](MD/VOLUME33.md) | The browser capture includes `000.html` and all six visible entries as `001.html`–`006.html` (`c.32:1`–`c.32:6`): the headnote, four draft ranges, and the printing proposal. The Previous link from `001.html` exposes `c.32:0`, but that preceding section is absent from `000.html`, the manifest, and the local files; its unavailable content was not reconstructed. These manuscript pages contain no standard semantic page markers. All 194 semantic footnote references have definitions. Sources `002.html`–`006.html` contain empty `transcriptImage` placeholders without URLs or local files; the image audit classified all fourteen locally saved candidates as `omit-noncontent`, with no `include`, `omit-scan`, or `uncertain`. `006.html` lacks the usual closing comment, but its safe fallback reaches the complete printing proposal before the footer. |
| 34 | *"Original Sin" Notebook* | [MD/VOLUME34.md](MD/VOLUME34.md) | The browser capture includes `000.html` and all five visible entries as `001.html`–`005.html` (`c.33:1`–`c.33:5`). The Previous link from `001.html` exposes `c.33:0`, but that preceding section is absent from `000.html`, the manifest, and the local files. The source explicitly marks manuscript pages 67–78 as missing; none of this unavailable material was reconstructed. These transcriptions contain no standard semantic page markers. All 186 semantic footnote references have definitions. Every source contains an empty `transcriptImage` placeholder without a URL or local file; the image audit classified all twelve locally saved candidates as `omit-noncontent`, with no `include`, `omit-scan`, or `uncertain`. `005.html` lacks the usual closing comment, but its safe fallback reaches the final paragraph, the blank back-cover notation, and the complete notes before the footer. |
| 35 | *No WJE Online volume published* | — | The official browser-visible WJE Online index proceeds directly from Vol. 34 (*"Original Sin" Notebook*) to Vol. 36 (*Sermon Notebooks*) and exposes no Vol. 35 link, navigation URL, or source content. No HTML, image manifest, or Markdown placeholder was invented. This official numbering gap is recorded explicitly in `metadata/archive-progress.json`. |
| 36 | *Sermon Notebooks* | [MD/VOLUME36.md](MD/VOLUME36.md) | The browser capture includes `000.html` and all three visible notebooks as `001.html`–`003.html` (`c.34:1`–`c.34:3`): notebooks XIV, XIX, and 45. The Previous link from `001.html` exposes `c.34:0`, but that preceding section is absent from `000.html`, the manifest, and the local files. Source notes explicitly mark missing leaves 3, 5–6, 9–10, and 12 in notebook XIV, and leaf 97 in notebook 45; none of this unavailable manuscript material was reconstructed. The transcriptions use literal bracketed leaf numbers rather than standard semantic page markers. All 56 semantic footnote references have definitions. The image audit classified all eight locally saved candidates as `omit-noncontent`, with no `include`, `omit-scan`, or `uncertain`. `003.html` lacks the usual closing comment, but its safe fallback reaches the final blank and missing-leaf notations before the footer. |
| 37 | *Documents on the Trinity, Grace and Faith* | [MD/VOLUME37.md](MD/VOLUME37.md) | The browser capture includes `000.html` and sixteen unique Yale sources as `001.html`–`016.html` (`c.35:1`–`c.35:16`); the navigation displays 22 labels because six nested labels resolve to already represented source URLs. The Previous link from `001.html` exposes `c.35:0`, but that preceding section is absent from the navigation, manifest, and local files. Source notes explicitly mark pages 101–116 missing from `001.html`, and leaves containing pages 19–20, 29–30, and 33–36 missing from `005.html`; further manuscript damage and lacunae are retained without reconstruction. All 84 semantic page markers are blank in the Yale source, so no numeric range is asserted. All 639 semantic footnote references have definitions, while eighteen source definitions without references are preserved explicitly. Sources `001.html`–`009.html` contain empty `transcriptImage` placeholders without URLs or local files. The image audit classified all 34 locally saved candidates as `omit-noncontent`, with no `include`, `omit-scan`, or `uncertain`. `016.html` lacks the usual closing comment, but its safe fallback reaches the final visible entry ending in the source's literal `[...]` before the footer. |
| 38 | *Dismissal and Post-Dismissal Documents* | [MD/VOLUME38.md](MD/VOLUME38.md) | The browser capture includes `000.html` and sixteen unique Yale sources as `001.html`–`016.html` (`c.36:1`–`c.36:16`); the navigation displays seventeen labels because the nested second Farewell Sermon fragment shares `c.36:15` with the first. The Previous link from `001.html` exposes `c.36:0`, but that preceding section is absent from the navigation, manifest, and local files. A source note in `006.html` explicitly says that the bottom half of the second manuscript leaf is missing; this unavailable material and other source-indicated damage are not reconstructed. All twelve semantic page markers are blank in the Yale source, so no numeric range is asserted. All 500 semantic footnote references have definitions. Eleven specified sources contain empty `transcriptImage` placeholders without URLs or local files. The image audit classified all 34 locally saved candidates as `omit-noncontent`, with no `include`, `omit-scan`, or `uncertain`. The validator's one heading notice results from Yale's malformed navigation label omitting the italicized words *Farewell Sermon*; both fragments and the complete source heading are present in Markdown. `016.html` lacks the usual closing comment, but its safe fallback reaches the complete final visible fragment before the footer. |
| 39 | *Church and Pastoral Documents* | [MD/VOLUME39.md](MD/VOLUME39.md) | The browser capture includes `000.html` and all 29 aggregate Yale sources as `001.html`–`029.html` (`c.37:1`–`c.37:29`). The thirtieth visible navigation label, `c.37:1:1`, is already fully embedded in aggregate `001.html` and appears as its substantive child heading. The Previous link from `001.html` exposes `c.37:0`, but that preceding section is absent from the navigation, manifest, and local files. Source `003.html` explicitly marks the sheet containing question numbers 58–100 as missing, and `029.html` states that a document intended to precede the council's advice is missing; neither unavailable item was reconstructed. These manuscript documents contain no standard semantic page markers. All 289 semantic footnote references have definitions. Ten specified sources contain empty `transcriptImage` placeholders without URLs or local files. The image audit classified all 60 locally saved candidates as `omit-noncontent`, with no `include`, `omit-scan`, or `uncertain`. `029.html` lacks the usual closing comment, but its safe fallback reaches the complete final visible advice before the footer. |
| 40 | *Autobiographical and Biographical Documents* | [MD/VOLUME40.md](MD/VOLUME40.md) | After a site-wide Yale 504, two failed invocations were retained in the manifest and `--resume` completed the capture without overwriting sources; `000.html` records nine cumulative attempts. The capture includes all 22 visible sources as `001.html`–`022.html` (`c.38:1`–`c.38:22`). The Previous link from `001.html` exposes unavailable `c.38:0`. Source `003.html` explicitly marks lacunae at manuscript pages 9–26, 29–32, 39–46, 49–52, 57–58, and 69–70; `015.html` reports about twelve lost lines and further text obliterated by a tear. None of this missing text was reconstructed. All six semantic page markers are blank in Yale's source, so no numeric range is asserted. All 272 semantic footnote references have definitions. Seventeen specified sources contain empty `transcriptImage` placeholders without URLs or local files. The image audit classified all 46 local candidates as `omit-noncontent`, with no `include`, `omit-scan`, or `uncertain`. The validator's two heading notices are source-label normalization differences; the complete Diary fragment heading and text are present. `022.html` lacks the usual closing comment, but its safe fallback reaches complete Resolution 70 before the footer. |
| 41 | *Family Writings and Related Documents* | [MD/VOLUME41.md](MD/VOLUME41.md) | The browser capture includes `000.html` and all twelve aggregate sources as `001.html`–`012.html` (`c.39:1`–`c.39:12`). The thirteenth navigation label, child `c.39:10:1`, is fully embedded in `010.html` as the edited version. The Previous link from `001.html` exposes unavailable `c.39:0`. Source damage and shorn manuscript edges in `001.html`, `010.html`, `011.html`, and `012.html` are preserved through brackets and literal ellipses; no absent wording was inferred. All nine semantic page markers are blank in Yale's source, so no numeric range is asserted. All 87 semantic footnote references have definitions, and the duplicate unreferenced source definition `001-note31` remains explicit. The image audit classified all 26 local candidates as `omit-noncontent`, with no content-image placeholders, `include`, `omit-scan`, or `uncertain`. `012.html` lacks the usual closing comment, but its safe fallback reaches the source's final `[rest of p. blank]` notation before the footer. |
| 42 | *Sermons, Series II, 1723–1727* | [MD/VOLUME42.md](MD/VOLUME42.md) | The browser capture includes `000.html` and all 35 visible sermons as `001.html`–`035.html` (`c.40:1`–`c.40:35`). The Previous link from `001.html` exposes unavailable `c.40:0`. Source annotations in `023.html` and `030.html` explicitly mark manuscript damage; brackets and source wording are retained without inferring lost text. These sermon manuscripts contain no standard semantic page markers. All 839 semantic footnote references have definitions, and unreferenced source definition `007-note20` remains explicit. Thirty-four sources contain empty `transcriptImage` placeholders without URLs or local files. The image audit classified all 72 local candidates as `omit-noncontent`, with no `include`, `omit-scan`, or `uncertain`. `035.html` lacks the usual closing comment, but its safe fallback reaches the final visible sermon notation `[L. 4v. empty]` before the footer. |
| 43 | *Sermons, Series II, 1728–1729* | [MD/VOLUME43.md](MD/VOLUME43.md) | The browser capture includes `000.html` and all 38 consecutive aggregate Yale sources as `001.html`–`038.html` (`c.41:1`–`c.41:38`). The navigation's additional 23 technical descendants under `c.41:12` are rendered inside aggregate `012.html`; the edited sermon and nested source structure are preserved there. The Previous link from `001.html` exposes unavailable `c.41:0`. Source `006.html` explicitly marks two groups of several lines missing because of extensive manuscript damage, and further source damage annotations are retained without reconstructing text. These sermon manuscripts contain no standard semantic page markers. All 1,377 semantic footnote references have definitions. Thirteen sources contain empty `transcriptImage` placeholders without URLs or local files. The image audit classified all 78 local candidates as `omit-noncontent`, with no `include`, `omit-scan`, or `uncertain`. Validator heading notices are technical or source-label normalization differences: the `[sermon]`/`[NA]` descendants are structural labels, `c.41:13` contains its full transcript despite having no substantive navigation label, and sermons 074–096 are present with their source headings. `038.html` lacks the usual closing comment, but its safe fallback reaches the source's final visible fragment ending `yet more Conformed to him &` before the footer. |
| 44 | *Sermons, Series II, 1729* | [MD/VOLUME44.md](MD/VOLUME44.md) | The browser capture includes `000.html` and all 52 visible sources as `001.html`–`052.html` (`c.42:1`–`c.42:52`). Yale orders the sermon labels 100–149 followed by 097–099; this source order is preserved and every navigation heading is present in Markdown. The Previous link from `001.html` exposes unavailable `c.42:0`. Source `045.html` explicitly marks manuscript damage, retained without inferring absent wording. These sermon manuscripts contain no standard semantic page markers. All 1,237 semantic footnote references have definitions. Fifty-one sources contain empty `transcriptImage` placeholders without URLs or local files. The image audit classified all 106 local candidates as `omit-noncontent`, with no `include`, `omit-scan`, or `uncertain`. `052.html` lacks the usual closing comment, but its safe fallback reaches the final visible underscore following `honour Glory & Immortality` before the footer. |
| 45 | *Sermons, Series II, 1729–1731* | [MD/VOLUME45.md](MD/VOLUME45.md) | The browser capture includes `000.html` and all 39 visible sermons as `001.html`–`039.html` (`c.43:1`–`c.43:39`), with every navigation heading present in Markdown. The Previous link from `001.html` exposes unavailable `c.43:0`. Source `038.html` explicitly marks manuscript damage, retained without inferring absent wording. These sermon manuscripts contain no standard semantic page markers. All 1,125 semantic footnote references have definitions; unreferenced source definitions `038-note43`, `038-note49`, and `038-note55` remain explicit. Every sermon source contains an empty `transcriptImage` placeholder without a URL or local file. The image audit classified all 80 local candidates as `omit-noncontent`, with no `include`, `omit-scan`, or `uncertain`. Literal vertical strokes in `014.html` are preserved as escaped Markdown prose rather than malformed table rows; the generator now covers this case with a regression test. `039.html` lacks the usual closing comment, but its safe fallback reaches the final visible line ending `fervent in sp serving ----` before the footer. |
| 46 | *Sermons, Series II, 1731–1732* | [MD/VOLUME46.md](MD/VOLUME46.md) | The browser capture includes `000.html` and all 39 visible sermons as `001.html`–`039.html` (`c.44:1`–`c.44:39`), with every navigation heading present in Markdown. The Previous link from `001.html` exposes unavailable `c.44:0`. Sources `003.html`–`007.html` and `032.html` explicitly mark manuscript damage or missing text, including damaged margins spanning three lines in `006.html`; all available brackets and annotations are preserved without reconstruction. These sermon manuscripts contain no standard semantic page markers. All 1,150 semantic footnote references have definitions. Thirty-eight sources contain empty `transcriptImage` placeholders without URLs or local files. The image audit classified all 80 local candidates as `omit-noncontent`, with no `include`, `omit-scan`, or `uncertain`. `039.html` lacks the usual closing comment, but its safe fallback reaches the final source notation `[rest of L. 10v. empty]` before the footer. |
| 47 | *Sermons, Series II, 1731–1732* | [MD/VOLUME47.md](MD/VOLUME47.md) | The browser capture includes `000.html` and all 41 aggregate Yale sources as `001.html`–`041.html` (`c.45:1`–`c.45:41`). Four additional child labels (`c.45:29:1`, `c.45:39:1`, `c.45:40:1`, and `c.45:41:1`) are embedded in their parent sources, and every navigation heading is present in Markdown. The Previous link from `001.html` exposes unavailable `c.45:0`. Nine specified sources explicitly mark manuscript damage; `004.html` says that the remainder of a leaf is torn away, and none of the absent wording was reconstructed. The 44 page markers comprise `362` followed by 23 literal `pagenum` placeholders in `029.html`, then 423–428, 323–329, and 331–337 in source order; numeric page 330 is absent. All 1,053 semantic footnote references have definitions. Thirty-seven sources contain empty `transcriptImage` placeholders without URLs or local files. The image audit classified all 84 local candidates as `omit-noncontent`, with no `include`, `omit-scan`, or `uncertain`. `041.html` lacks the usual closing comment, but its safe fallback reaches the source's final annotation `[sic]no close quotation mark in text]` before the footer. |
| 48 | *Sermons, Series II, 1733* | [MD/VOLUME48.md](MD/VOLUME48.md) | The browser capture includes `000.html` and all 42 aggregate Yale sources as `001.html`–`042.html` (`c.46:1`–`c.46:42`). The additional child label `c.46:14:1` is embedded in aggregate `014.html`, and every navigation heading is present in Markdown. The Previous link from `001.html` exposes unavailable `c.46:0`. Nineteen specified sources (`003.html`, `008.html`, `010.html`, `012.html`, `015.html`, `016.html`, `018.html`, `020.html`, `021.html`, `025.html`, `027.html`–`029.html`, `031.html`, `032.html`, `036.html`–`038.html`, and `041.html`) explicitly mark illegible, faded, torn, or damaged manuscript text; the available annotations are preserved without reconstruction. These sermon manuscripts contain no standard semantic page markers. All 1,839 semantic footnote references have definitions, and unreferenced source definition `014-note18` remains explicit. Forty-one sources contain empty `transcriptImage` placeholders without URLs or local files. The image audit classified all 86 local candidates as `omit-noncontent`, with no `include`, `omit-scan`, or `uncertain`. `042.html` lacks the usual closing comment, but its safe fallback reaches the final source annotations `[L. 9v. is from a different sermon]` and `[signature of John Edwards at right angles]` before the footer. |
| 49 | *Sermons, Series II, 1734* | [MD/VOLUME49.md](MD/VOLUME49.md) | The browser capture includes `000.html` and all 41 aggregate Yale sources as `001.html`–`041.html` (`c.47:1`–`c.47:41`). Two additional child labels, `c.47:14:1` and `c.47:36:1`, are embedded in their parent sources, and every navigation heading is present in Markdown. The Previous link from `001.html` exposes unavailable `c.47:0`. Eighteen specified sources (`001.html`–`009.html`, `011.html`–`013.html`, `017.html`, `020.html`, `026.html`, `028.html`, `037.html`, and `041.html`) mark at least one missing page, a manuscript hole, faded or obliterated text, or other damage; the surviving annotations are preserved without reconstruction. The only four semantic page markers are literal `page number here` placeholders, two each in `014.html` and `036.html`, so no numeric page range is asserted. All 1,395 semantic footnote references have definitions. Thirty-nine sources contain empty `transcriptImage` placeholders without URLs or local files. The image audit classified all 84 local candidates as `omit-noncontent`, with no `include`, `omit-scan`, or `uncertain`. `041.html` lacks the usual closing comment, but its safe fallback reaches the final `[half a page blank]` and `[L. 18v.]` annotations before the footer. |
| 50 | *Sermons, Series II, 1735* | [MD/VOLUME50.md](MD/VOLUME50.md) | The browser capture includes `000.html` and all 27 aggregate Yale sources as `001.html`–`027.html` (`c.48:1`–`c.48:27`). Child label `c.48:12:1` is embedded in aggregate `012.html`, and every navigation heading is present in Markdown. The Previous link from `001.html` exposes unavailable `c.48:0`. Fifteen specified sources (`002.html`–`005.html`, `007.html`, `009.html`, `013.html`–`015.html`, `018.html`, `020.html`, `021.html`, and `025.html`–`027.html`) preserve blank manuscript holes or explicit missing and damaged text, including two `remainder of page missing` annotations in `013.html`; none was reconstructed. The fourteen printed page markers in `012.html` run 39 and 41–53, with numeric marker 40 absent. All 1,282 semantic footnote references have definitions. Twenty-six sources contain empty `transcriptImage` placeholders without URLs or local files. The image audit classified all 56 local candidates as `omit-noncontent`, with no `include`, `omit-scan`, or `uncertain`. A compact deletion `--<del>1</del>--` in `027.html` is retained as prose rather than a false page marker; the generator now covers this case with a regression test. `027.html` lacks the usual closing comment, but its safe fallback reaches `[Remainder of leaf blank]` before the footer. |
| 51 | *Sermons, Series II, 1736* | [MD/VOLUME51.md](MD/VOLUME51.md) | The browser capture includes `000.html` and all 41 visible Yale sources as `001.html`–`041.html` (`c.49:1`–`c.49:41`), with every navigation heading present in Markdown; Yale's sermon labels intentionally jump from 401 to 404. The Previous link from `001.html` exposes unavailable `c.49:0`. Twenty-three specified sources preserve literal empty-bracket manuscript lacunae, `028.html` marks a `page rip`, and incomplete source annotations in `006.html`, `013.html`, and `024.html` remain literal; no absent wording was inferred. These sermon manuscripts contain no printed page markers, while `041.html` preserves 51 leaf annotations from `L.1v` through `L.26v`. All 1,468 semantic footnote references have definitions. Every sermon source contains an empty `transcriptImage` holder without a usable URL or local content image. The image audit classified all 84 local candidates as `omit-noncontent`, with no `include`, `omit-scan`, or `uncertain`. `041.html` lacks the usual closing comment, but its safe fallback reaches the final damaged marriage-posting fragment ending `Sep Sam. Mather Town Clerk` before the footer. |
| 52 | *Sermons, Series II, 1737* | [MD/VOLUME52.md](MD/VOLUME52.md) | The browser capture includes `000.html` and all 30 visible Yale sources as `001.html`–`030.html` (`c.50:1`–`c.50:30`), with every navigation heading present in Markdown. Yale's sermon labels run 417–434 and then omit 435, 436, 438, 440, 444, 447, and 452 in the visible source sequence. The Previous link from `001.html` exposes unavailable `c.50:0`. Literal bracketed unreadable fragments in `001.html`, `005.html`, `008.html`, and `030.html` are retained without reconstruction; twelve other sources explicitly annotate blank leaves or lines. The manuscripts contain no printed page markers, while `030.html` reaches leaf annotation `L. 13v.`. All 1,107 semantic footnote references have definitions. Sources `001.html`–`029.html` contain empty `transcriptImage` elements without URLs or local content images. The image audit classified all 62 local candidates as `omit-noncontent`, with no `include`, `omit-scan`, or `uncertain`. `030.html` lacks the usual closing comment, but its safe fallback reaches the complete final sentence ending `that where he is there you may be also.` before the footer. |
| 53 | *Sermons, Series II, 1738, and Undated, 1734–1738* | [MD/VOLUME53.md](MD/VOLUME53.md) | The browser capture includes `000.html` and all 28 visible Yale sources as `001.html`–`028.html` (`c.51:1`–`c.51:28`), with every navigation heading present in Markdown. Yale's sermon labels contain gaps and two consecutive entries both numbered 467 before continuing through 498d. The Previous link from `001.html` exposes unavailable `c.51:0`. Source `012.html` explicitly marks manuscript damage at leaf 13v, while `009.html` records wholly blank leaves and page portions; no unavailable wording was reconstructed. The manuscripts contain no printed page markers and instead preserve leaf annotations throughout. All 1,138 semantic footnote references have definitions. Every source contains an empty `transcriptImage` element without a URL or local content image. The image audit classified all 58 local candidates as `omit-noncontent`, with no `include`, `omit-scan`, or `uncertain`. `028.html` is structurally truncated in Yale's rendered source: it lacks footnotes, lower navigation, the repeated citation, and `END OF CONTEXT AREA`; the fallback preserves everything available through `[blank][L. 6]` before the footer, without reconstructing a continuation. |
| 54 | *Sermons, Series II, 1739* | [MD/VOLUME54.md](MD/VOLUME54.md) | The browser capture includes `000.html` and all 29 aggregate Yale sources as `001.html`–`029.html` (`c.52:1`–`c.52:29`). Child labels `c.52:16:1` and `c.52:25:1` are embedded in their parent sources, and every navigation heading is present in Markdown. Yale's sermon labels contain gaps and repeat 516 for consecutive entries. The Previous link from `001.html` exposes unavailable `c.52:0`. Nine specified sources explicitly annotate blank pages, leaves, or line space, and `020.html` preserves a literal `[lost]` annotation; uncertain bracketed readings elsewhere remain exactly as rendered, without reconstruction. The only 49 semantic page markers are blank Yale placeholders—17 in `009.html` and 32 in `020.html`—so no numeric range is asserted. All 1,116 semantic footnote references have definitions. Twenty-five specified sources contain empty `transcriptImage` elements without URLs or local content images. The image audit classified all 60 local candidates as `omit-noncontent`, with no `include`, `omit-scan`, or `uncertain`. `029.html` ends after the fragment closing with `our Lusts` and then enters the site footer without lower navigation, repeated citation, footnotes, or `END OF CONTEXT AREA`; the fallback preserves only the available text and does not reconstruct a continuation. |
| 55 | *Sermons, Series II, January–June 1740* | [MD/VOLUME55.md](MD/VOLUME55.md) | The browser capture includes `000.html` and all 24 aggregate Yale sources as `001.html`–`024.html` (`c.53:1`–`c.53:24`). Child label `c.53:20:1` is embedded in its parent source, every navigation heading is present in Markdown, and Yale's visible sermon labels contain gaps between 530 and 557. The Previous link from `001.html` exposes unavailable `c.53:0`. Seven specified sources (`001.html`–`003.html`, `007.html`, `012.html`, `017.html`, and `022.html`) explicitly annotate blank pages, leaves, or leaf portions; none was reconstructed. The only 36 semantic page markers are blank Yale placeholders in `004.html`, so no numeric range is asserted. All 631 semantic footnote references have definitions. Five specified sources contain empty `transcriptImage` elements without URLs or local content images. The image audit classified all 50 local candidates as `omit-noncontent`, with no `include`, `omit-scan`, or `uncertain`. `024.html` is structurally truncated in Yale's rendered source midway through the sentence ending `if they are so foolish as to neglect it till their`; it then enters the site footer without the remainder, footnotes, lower navigation, repeated citation, or `END OF CONTEXT AREA`. Only the available text was converted, without reconstruction. |
| 56 | *Sermons, Series II, July–December 1740* | [MD/VOLUME56.md](MD/VOLUME56.md) | The browser capture includes `000.html` and all 21 aggregate Yale sources as `001.html`–`021.html` (`c.54:1`–`c.54:21`). Child label `c.54:16:1` is embedded in its parent source, every navigation heading is present in Markdown, and Yale's visible sermon labels contain gaps and a repeated 579a label in the parent-child structure. The Previous link from `001.html` exposes unavailable `c.54:0`. Source `017.html` explicitly marks manuscript damage, while `020.html` and `021.html` each retain a `missing word` annotation; no unavailable wording was reconstructed. Three other sources describe blank pages or lines. These manuscripts contain no semantic printed page markers, while `021.html` reaches leaf annotation `L. 14r.`. All 661 semantic footnote references have definitions. Fifteen specified sources contain empty `transcriptImage` elements without URLs or local content images. The image audit classified all 44 local candidates as `omit-noncontent`, with no `include`, `omit-scan`, or `uncertain`. `021.html` lacks the usual closing comment, but its safe fallback reaches the complete final sentence ending `discouraged by the Length of the way.` before the footer. |
| 57 | *Sermons, Series II, January–June 1741* | [MD/VOLUME57.md](MD/VOLUME57.md) | The browser capture includes `000.html` and all nine visible Yale sources as `001.html`–`009.html` (`c.55:1`–`c.55:9`), with every navigation heading present in Markdown; Yale's sermon labels run nonconsecutively from 587 through 620. The Previous link from `001.html` exposes unavailable `c.55:0`. Sources `001.html` and `003.html` explicitly mark manuscript damage, while `007.html` preserves `missing word` and abbreviated `miss. word` annotations; four sources also describe blank pages, leaves, columns, or lines. No unavailable wording was reconstructed. These manuscripts contain no semantic printed page markers, while `009.html` reaches leaf annotation `L. 11v.`. All 524 semantic footnote references have definitions. Every source contains an empty `transcriptImage` element without a URL or local content image. The image audit classified all 20 local candidates as `omit-noncontent`, with no `include`, `omit-scan`, or `uncertain`. `009.html` lacks the usual closing comment, but its safe fallback reaches the complete final sentence ending `Causing his Great trumpet to be blown.` before the footer. |
| 58 | *Sermons, Series II, July–December 1741* | [MD/VOLUME58.md](MD/VOLUME58.md) | The browser capture includes `000.html` and all four visible Yale sources as `001.html`–`004.html` (`c.56:1`–`c.56:4`), with every navigation heading present in Markdown; Yale's sermon labels are 626, 627, 636, and 646. The Previous link from `001.html` exposes unavailable `c.56:0`. Source `004.html` explicitly preserves a manuscript-damage annotation, without reconstruction. These manuscripts contain no semantic printed page markers, while `004.html` reaches leaf annotation `L. 22v.`. All 50 semantic footnote references have definitions. Every source contains an empty `transcriptImage` element without a URL or local content image. The image audit classified all 10 local candidates as `omit-noncontent`, with no `include`, `omit-scan`, or `uncertain`. `004.html` lacks expanded footnotes, lower navigation, the repeated citation, and `END OF CONTEXT AREA`; its safe fallback nevertheless reaches the complete final sentence ending `you are all Enemies especially foolish sinners.` before the footer. |
| 59 | *Sermons, Series II, January–June 1742* | [MD/VOLUME59.md](MD/VOLUME59.md) | The browser capture includes `000.html` and both visible Yale sources as `001.html`–`002.html` (`c.57:1`–`c.57:2`), with both navigation headings present in Markdown; Yale's sermon labels are 656 and 668. The Previous link from `001.html` exposes unavailable `c.57:0`. Source `002.html` explicitly preserves a `missing word` annotation, while both sources describe blank columns, lines, pages, or leaf portions; no unavailable wording was reconstructed. These manuscripts contain no semantic printed page markers, while `002.html` reaches leaf annotation `L. 6v.`. All 39 semantic footnote references have definitions. Both sources contain empty `transcriptImage` elements without URLs or local content images. The image audit classified all six local candidates as `omit-noncontent`, with no `include`, `omit-scan`, or `uncertain`. `002.html` lacks lower navigation, the repeated citation, a footnote section, and `END OF CONTEXT AREA`; its safe fallback nevertheless reaches the complete final sentence ending `will carry the soul above all sorrow.` before the footer. |
| 60 | *Sermons, Series II, July–December 1742, and Undated, 1739–1742* | [MD/VOLUME60.md](MD/VOLUME60.md) | The browser capture includes `000.html` and all four aggregate Yale sources as `001.html`–`004.html` (`c.58:1`–`c.58:4`). Child label `c.58:2:1` is embedded in its parent source, every navigation heading is present in Markdown, and Yale repeats sermon label 679 for the transcript and edited headnote before labels 693e and 693p. The Previous link from `001.html` exposes unavailable `c.58:0`. Source `002.html` retains a literal `deleted and undecipherable` annotation without reconstruction. The only five semantic page markers are `pagenum` placeholders in `002.html`, so no numeric range is asserted; `004.html` reaches leaf annotation `L. 13v.`. All 92 semantic footnote references have definitions. Three specified sources contain empty `transcriptImage` elements without URLs or local content images. The image audit classified all 10 local candidates as `omit-noncontent`, with no `include`, `omit-scan`, or `uncertain`. `004.html` lacks expanded footnotes, lower navigation, the repeated citation, and `END OF CONTEXT AREA`; its safe fallback nevertheless reaches the complete final sentence ending `the paradise of G. that is planted there.` before the footer. |
| 61 | *Sermons, Series II, 1743* | [MD/VOLUME61.md](MD/VOLUME61.md) | The browser capture includes `000.html` and all seven visible Yale sources as `001.html`–`007.html` (`c.59:1`–`c.59:7`), with every navigation heading present in Markdown; Yale's sermon labels run nonconsecutively from 704 through 724. The Previous link from `001.html` exposes unavailable `c.59:0`. Source `002.html` explicitly marks manuscript damage and notes leaves lost in 1750; `005.html` preserves a literal lacuna, while four sources describe blank pages, columns, leaves, or reserved space. No unavailable wording was reconstructed. These manuscripts contain no semantic printed page markers. All 558 semantic footnote references have definitions. Three specified sources contain empty `transcriptImage` elements without URLs or local content images. The image audit classified all 16 local candidates as `omit-noncontent`, with no `include`, `omit-scan`, or `uncertain`. `007.html` is structurally truncated in Yale's rendered source during the opening outline after `There is a dear affection between X & them`, at leaf `L. 2r.`; it then enters the footer without the sermon continuation, footnotes, lower navigation, repeated citation, or `END OF CONTEXT AREA`. Only the available text was converted. |
| 62 | *Sermons, Series II, 1744* | [MD/VOLUME62.md](MD/VOLUME62.md) | The browser capture includes `000.html` and all eight visible Yale sources as `001.html`–`008.html` (`c.60:1`–`c.60:8`), with every navigation heading present in Markdown; Yale's sermon labels run nonconsecutively from 737 through 764, and the first source pairs Acts 20:24 with secondary text Ps. 66:18 exactly as rendered. The Previous link from `001.html` exposes unavailable `c.60:0`. Sources `002.html` and `008.html` explicitly describe blank manuscript columns; no unavailable wording was reconstructed. These manuscripts contain no semantic printed page markers, while `008.html` reaches leaf annotation `L. 26v.`. All 296 semantic footnote references have definitions. Every source contains an empty `transcriptImage` element without a URL or local content image. The image audit classified all 18 local candidates as `omit-noncontent`, with no `include`, `omit-scan`, or `uncertain`. `008.html` lacks lower navigation, the repeated citation, a footnote section, and `END OF CONTEXT AREA`; its safe fallback nevertheless reaches the complete final sentence ending `Gods work in Glorifying is particular.` before the footer. |
| 63 | *Sermons, Series II, 1745* | [MD/VOLUME63.md](MD/VOLUME63.md) | The browser capture includes `000.html` and all six visible Yale sources as `001.html`–`006.html` (`c.61:1`–`c.61:6`), with every navigation heading present in Markdown; Yale's sermon labels run nonconsecutively from 776 through 800. The Previous link from `001.html` exposes unavailable `c.61:0`. Source `002.html` explicitly marks a missing leaf or leaves, and `005.html` preserves manuscript-damage annotations; no unavailable wording was reconstructed. Sources `004.html`–`006.html` also describe blank space, including page and column portions cut off before Edwards wrote the sermon. These manuscripts contain no semantic printed page markers, while `006.html` reaches leaf annotation `L. 20v.`. All 401 semantic footnote references have definitions; `003-note1` consists only of Yale's literal empty brackets and remains unfilled. Five specified sources contain empty `transcriptImage` elements without URLs or local content images. The image audit classified all 14 local candidates as `omit-noncontent`, with no `include`, `omit-scan`, or `uncertain`. `006.html` lacks lower navigation, the repeated citation, a footnote section, and `END OF CONTEXT AREA`; its safe fallback nevertheless reaches the complete closing exhortation ending `wisely improve this day of Gods Patience` before the footer. |
| 64 | *Sermons, Series II, 1746* | [MD/VOLUME64.md](MD/VOLUME64.md) | The browser capture includes `000.html` and all twelve visible Yale sources as `001.html`–`012.html` (`c.62:1`–`c.62:12`), with every navigation heading present in Markdown; Yale's sermon labels run nonconsecutively from 812 through 851. The Previous link from `001.html` exposes unavailable `c.62:0`. Source `004.html` preserves a manuscript-damage annotation, while several sources describe blank columns, pages, or line ranges; no unavailable wording was reconstructed. The only 107 semantic page markers are blank Yale placeholders in five specified sources, so no numeric range is asserted; `012.html` reaches leaf annotation `L. 12v.`. All 341 semantic footnote references have definitions, including nine source-empty definitions in `001.html`, `006.html`, and `009.html` that remain unfilled. Six specified sources contain empty `transcriptImage` elements without URLs or local content images. The image audit classified all 26 local candidates as `omit-noncontent`, with no `include`, `omit-scan`, or `uncertain`. `012.html` is structurally truncated in Yale's rendered source during a biblical allusion after `who shall ascend into Heaven -`; it then enters the footer without the continuation, footnotes, lower navigation, repeated citation, or `END OF CONTEXT AREA`. Only the available fragment was converted. |
| 65 | *Sermons, Series II, 1747* | [MD/VOLUME65.md](MD/VOLUME65.md) | The browser capture includes `000.html` and all 33 visible Yale sources as `001.html`–`033.html` (`c.63:1`–`c.63:33`), with every navigation heading present in Markdown; Yale's sermon labels run from 853 through 886 with 860 absent. The Previous link from `001.html` exposes unavailable `c.63:0`. Sources `012.html` and `014.html` explicitly mark missing manuscript leaves, and six specified sources preserve manuscript-damage annotations; no unavailable wording was reconstructed. Numerous sources describe blank columns, pages, leaves, or line ranges. These manuscripts contain no semantic printed page markers, while `033.html` reaches source notation `L. 15`. All 2,715 semantic footnote references have definitions; `002-note101` consists only of Yale's literal empty brackets and remains unfilled. Every source contains an empty `transcriptImage` element without a URL or local content image. The image audit classified all 68 local candidates as `omit-noncontent`, with no `include`, `omit-scan`, or `uncertain`. `033.html` lacks lower navigation, the repeated citation, a footnote section, and `END OF CONTEXT AREA`; its safe fallback reaches the final available source notations `[printed text][L. 15]` before the footer, without reconstructing a continuation. |
| 66 | *Sermons, Series II, 1748* | [MD/VOLUME66.md](MD/VOLUME66.md) | The browser capture includes `000.html` and all 32 visible Yale sources as `001.html`–`032.html` (`c.64:1`–`c.64:32`), with every navigation heading present in Markdown; Yale repeats sermon label 898 across four parts and omits labels 901 and 911 in the visible sequence from 887 through 917. The Previous link from `001.html` exposes unavailable `c.64:0`. Sources `007.html` and `011.html` preserve manuscript-damage annotations; `008.html` ends with `[MS incomplete]`, while `011.html` and `016.html` end with `[MS breaks off]`. Numerous sources also describe blank columns, pages, leaves, or line ranges; no unavailable wording was reconstructed. These manuscripts contain no semantic printed page markers, while `032.html` reaches leaf annotation `L. 8v.`. All 1,906 semantic footnote references have definitions; `004-note8` and `013-note12` consist only of Yale's literal empty brackets and remain unfilled. Every source contains an empty `transcriptImage` element without a URL or local content image. The image audit classified all 66 local candidates as `omit-noncontent`, with no `include`, `omit-scan`, or `uncertain`. `032.html` is structurally truncated in Yale's rendered source after `will go away & never will[?]`; it then enters the footer without the continuation, footnotes, lower navigation, repeated citation, or `END OF CONTEXT AREA`. Only the available fragment was converted. |
| 67 | *Sermons, Series II, 1749* | [MD/VOLUME67.md](MD/VOLUME67.md) | The browser capture includes `000.html` and all 28 aggregate Yale sources as `001.html`–`028.html` (`c.65:1`–`c.65:28`). Child label `c.65:15:1` is embedded in `015.html`, every navigation heading is present in Markdown, and Yale's visible sermon labels run from 918 through 947 with 925 and 933 absent. The Previous link from `001.html` exposes unavailable `c.65:0`. Source `023.html` preserves a manuscript-damage annotation; `004.html` preserves two `[JE om.]` annotations, and `004.html`, `005.html`, `010.html`, and `011.html` identify textual fragments. Twenty-three sources describe 88 blank columns, pages, leaves, or line ranges; no unavailable wording was reconstructed. The six semantic printed page markers 204–209 occur in the edited child text embedded in `015.html`; `028.html` reaches leaf annotation `L. 12v.`. All 1,585 semantic footnote references have nonempty definitions. Twenty-six specified sources contain empty `transcriptImage` elements without URLs or local content images. The image audit classified all 58 local candidates as `omit-noncontent`, with no `include`, `omit-scan`, or `uncertain`. `028.html` is structurally truncated in Yale's rendered source after `The wicked shall see & be grieved & gnash with his Teeth & melt away`; it then enters the footer without the continuation, footnotes, lower navigation, repeated citation, or `END OF CONTEXT AREA`. Only the available fragment was converted. |
| 68 | *Sermons, Series II, 1750* | [MD/VOLUME68.md](MD/VOLUME68.md) | The browser capture includes `000.html` and all 27 visible Yale sources as `001.html`–`027.html` (`c.66:1`–`c.66:27`), with every navigation heading present in Markdown; Yale repeats sermon label 955 across five parts and omits labels 954 and 963 in the visible sequence from 948 through 972. Yale also renders visible/technical label mismatches for Jer. 17:5-6(b)/17'5-6a in `004.html` and John 12:13/John 1'12-13 in `026.html`; both forms are preserved. The Previous link from `001.html` exposes unavailable `c.66:0`. Sources `006.html`, `007.html`, `012.html`, `016.html`, and `022.html` preserve a torn manuscript, an ending manuscript, several damage readings, one or more missing leaves, and a folded manuscript corner; `008.html` and `018.html` preserve six `[JE om.]` annotations. Twenty sources describe 71 blank columns, pages, leaves, or line ranges; no unavailable wording was reconstructed. These manuscripts contain no semantic printed page markers, while `027.html` reaches leaf annotation `L. 20r.`. All 1,956 semantic footnote references have nonempty definitions. Twenty-six specified sources contain empty `transcriptImage` elements without URLs or local content images. The image audit classified all 56 local candidates as `omit-noncontent`, with no `include`, `omit-scan`, or `uncertain`. `027.html` is structurally truncated in Yale's rendered source after `while destitute of Preaching dont neglect social worship on the sabbath`; it then enters the footer without the continuation, footnotes, lower navigation, repeated citation, or `END OF CONTEXT AREA`. Only the available fragment was converted. |

Markdown documents contain only content present in the local capture.
